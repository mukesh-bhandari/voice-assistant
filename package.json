{
  "name": "voice-asistant",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o index.css --minify",
    "watch:css": "tailwindcss -i src/input.css -o index.css --watch",
    "start": "npm run build:css && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "electron": "^36.5.0"
  },
  "dependencies": {
    "tailwindcss": "^4.3.3"
  }
}
